import { View, Text } from "react-native";

function Primarybtn({children}){
    return (
        <View>
            <Text>{children}</Text>
        </View>
    );
}

export default Primarybtn;